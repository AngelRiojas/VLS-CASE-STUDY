package com.vls.vls_backend_cs;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class VlsBackendCsApplication {
    // The spring beans are created and managed by Spring Core Container.
    // The spring core container manages the life cycle of spring beans
    public static void main(String[] args) {
        SpringApplication.run(VlsBackendCsApplication.class, args);
    }

}
